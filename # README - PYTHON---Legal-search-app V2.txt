list_documents() — See all indexed document titles.

delete_document(title) — Remove documents by their title.

reindex_documents(docs) — Rebuild the index from a new list of documents.

UX enhancements in search_query() to show score and number of results.
